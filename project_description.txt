
Laboratório 1: Mininet
Condições de conclusão
Instruções Gerais
Nesta tarefa, você aprenderá o básico sobre o protocolo OpenFlow e sobre a execução do Mininet em uma máquina virtual VirtualBox.

Primeiro, instale o VirtualBox no seu computador. Em seguida, baixe a VM pronta do mininet através do link https://github.com/mininet/mininet/releases/download/2.3.0/mininet-2.3.0-210211-ubuntu-20.04.1-legacy-server-amd64-ovf.zip e importe para o virtualbox. 

Caso esteja utilizando o Gerenciador de Máquinas Virtuais do Linux (QEMU/KVM), crie uma nova máquina virtual, selecione a opção "Import existing disk image", e selecione o arquivo .vmdk do zip acima.

Para logar na VM as credenciais são:

login: mininet

senha: mininet

Depois, siga as instruções do tutorial Mininet abaixo (considere que o Mininet já está instalado):

Tutorial Mininet: http://mininet.org/walkthrough/
Após entender o básico sobre Mininet, você aprenderá a utilizar a API em Python do Mininet para criar topologias e automatizar tarefas.


Esta tarefa será baseada no tutorial disponível em https://github.com/mininet/mininet/wiki/Introduction-to-Mininet

A primeira fase da tarefa consiste em seguir as instruções do tutorial Mininet e testar todos os códigos apresentados ao logo do tutorial na sua máquina virtual.

A segunda fase da tarefa, consiste em adaptar o código abaixo, especificamente você deve:

a) Modificar a função def build( self, n=2 ) para que ela gere uma topologia de rede inventada por você, com no mínimo 4 switches e 11 hosts.
b) Modificar a função def perfTest() para testar a largura de banda [ net.iperf( (h1, H) ) ] entre o host h1 e todos os demais hosts da rede.
#!/usr/bin/python

from mininet.topo import Topo
from mininet.net import Mininet
from mininet.node import CPULimitedHost
from mininet.link import TCLink
from mininet.util import dumpNodeConnections
from mininet.log import setLogLevel

class SingleSwitchTopo( Topo ):
    "Single switch connected to n hosts."
    def build( self, n=2 ):
	switch = self.addSwitch( 's1' )
	for h in range(n):
	    # Each host gets 50%/n of system CPU
	    host = self.addHost( 'h%s' % (h + 1),
		                 cpu=.5/n )
	    # 10 Mbps, 5ms delay, 2% loss, 1000 packet queue
	    self.addLink( host, switch, bw=10, delay='5ms', loss=2,
                          max_queue_size=1000, use_htb=True )

def perfTest( net ):
    # run simple performance test"
    print "Testing bandwidth between h1 and h4"
    h1, h4 = net.get( 'h1', 'h4' )
    net.iperf( (h1, h4) )
if __name__ == '__main__':
    # "Create network and"
    topo = SingleSwitchTopo( n=4 )
    net = Mininet( 
          topo=topo,           ## Topologia
	  host=CPULimitedHost, ## define limite de uso de CPU
          link=TCLink          ## define o tipo link
         )
    net.start()
    print "Dumping host connections"
    dumpNodeConnections( net.hosts ) 
    print "Testing network connectivity"
    net.pingAll()
    setLogLevel( 'info' ) 
    perfTest( net )
    net.stop()

Laboratorio 2: Ryu+Openflow

Instruções Gerais
Nesta tarefa, você aprenderá o básico sobre como usar os conceitos de SDN para desenvolver aplicações utilizando o controlador Ryu.

Utilizando a mesma VM da aula anterior, siga as instruções abaixo para a instalação do controlador Ryu:

1. Atualize o sistema operacional:

$ sudo apt update && sudo apt dist-upgrade -y

2. Reinicie a VM

3. Instale a versão 3.x do Python:

$ python -m pip install --upgrade pip

4. Reinicie a VM

5. Clone o repositório do Ryu:

$ git clone https://github.com/osrg/ryu.git

6. Acesse o diretório do Ryu:

$ cd ryu

7. Instale o Ryu:

$ pip install .  ou $ python3 -m pip install .

Em seguida, siga as instruções abaixo:

1) Entre na VM via SSH com a flag -Y. Exemplo: $ ssh -Y mininet@<ip_da_vm>. Inicie a emulação de um ambiente com 1 switch e 3 hosts e em seguida o wireshark.
$ sudo mn --topo single,3 --mac --controller remote --switch ovsk,protocols=OpenFlow13

2) Em uma nova janela, iniciar uma captura no wireshark escolhendo a interface any e aplicar o filtro "openflow_v4": $ sudo -E wireshark

3) No Mininet, testar a comunicação entre h1 e h2:
mininet> h1 ping -c 1 h2

O que acontece? Por que?

4) Em uma nova janela, iniciar o controlador Ryu
$ ryu-manager --verbose ryu.app.simple_switch_13

5) Verifique no wireshark as mensagens OpenFlow que aparecem na captura. Tente explicar cada uma. Para ajudar consulte a documentação a seguir: http://flowgrammable.org/sdn/openflow/message-layer/

6) No Mininet, testar a comunicação entre h1 e h2:
mininet> h1 ping -c 1 h2

7) Verifique no wireshark as mensagens OpenFlow que aparecem na captura. Tente explicar cada uma.

8) Verifique a entradas da tabela de fluxo do switch s1:

$ sudo ovs-ofctl dump-flows s1 -O OpenFlow13

9) No Mininet, testar novamente a comunicação entre h1 e h2:
mininet> h1 ping -c 1 h2

10) Verifique no wireshark as mensagens OpenFlow que aparecem na captura e explique.

11) No Mininet, testar a comunicação entre h1 e h3:
mininet> h1 ping -c 1 h3

12) Verifique a entradas da tabela de fluxo do switch s1:

$ sudo ovs-ofctl dump-flows s1 -O OpenFlow13

Para entender melhor as mensagens OpenFlow, leia o tutorial de OpenFlow em anexo que fazia parte de um tutorial publicado em http://sdnhub.org/tutorials/openflow-1-3/ (atualmente indisponível).

Depois, siga o tutorial de Ryu em anexo que fazia parte de um tutorial publicado em http://sdnhub.org/tutorials/ryu/ (atualmente indisponível).

Então, explique em detalhes o funcionamento do learning switch após estudar o material de apoio.

Module 4.3: Customizing Control-Plane Behavior (Part 1: Switching)
Capítulo 2 Ryu Book
Código disponível em: https://github.com/faucetsdn/ryu/blob/master/ryu/app/simple_switch_13.py
Tutorial para criar uma aplicação no Ryu: https://ryu.readthedocs.io/en/latest/writing_ryu_app.html
